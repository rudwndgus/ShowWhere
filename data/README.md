# Private runtime data

`brain-v2/` is created automatically for scrubbed events, verified outcomes, trajectories, evaluation runs, gold imports, and training exports. It is intentionally ignored by Git because real UI observations may contain private data.

Share only manually reviewed, synthetic samples through `fixtures/`. Never move raw screenshots, messages, documents, secrets, or generated training exports into a tracked directory.

