<Window x:Class="ShowWhere.Desktop.FloatingAssistantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ShowWhere.Desktop"
        Width="58" Height="58" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ShowActivated="False" Focusable="False"
        MouseLeftButtonDown="OnMouseLeftButtonDown"
        MouseLeftButtonUp="OnMouseLeftButtonUp"
        MouseMove="OnMouseMove">
  <Grid ToolTip="ShowWhere — 하고 싶은 일을 말해보세요">
    <Border CornerRadius="29" Background="#22000000" Padding="3">
      <local:AssistantGlyph />
    </Border>
    <Grid.ContextMenu>
      <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
        <MenuItem Header="{Binding PauseMenuText}" Command="{Binding TogglePauseCommand}" />
        <Separator />
        <MenuItem Header="종료" Command="{Binding ExitCommand}" />
      </ContextMenu>
    </Grid.ContextMenu>
  </Grid>
</Window>
